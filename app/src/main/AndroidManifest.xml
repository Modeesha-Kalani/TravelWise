<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
        <activity android:name=".TimeTable"></activity>
        <activity android:name=".AddPayments" />
        <activity android:name=".my_credit" />
        <activity android:name=".MainActivity" />
        <activity android:name=".addcredits" />
        <activity android:name=".requestticket" />
        <activity android:name=".createprofile" />
        <activity android:name=".toolbar" />
        <activity android:name=".myjourneylist" />
        <activity android:name=".DoPayment">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>